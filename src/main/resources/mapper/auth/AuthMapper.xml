<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.msa4meerkatgram.domain.auth.mapper.AuthMapper">
   <update id="updateRefreshToken">
       UPDATE users
       set refresh_token = #{refreshToken}, updated_at = now()
       where id = #{id}
   </update>
   <insert id="created" parameterType="com.msa4meerkatgram.domain.user.entities.User" useGeneratedKeys="true" keyProperty="id">
    insert into users(
        email, 
        password, 
        nick,
        role,
        provider,
        profile) 
    values(
       #{email},
        #{password},
        #{nick},
        #{role},
       #{provider},
       #{profile}
          )
   </insert>
   
</mapper>